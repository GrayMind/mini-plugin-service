const text = '~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~'
export default function (api, options) {
  console.log("plugin 3 run ==>", api.id);
  api.configProcess((content) => {
    content.push(text)
    content.unshift(text)
  })
}